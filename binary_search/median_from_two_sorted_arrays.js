const input1 = [0, 2, 5, 6, 8];
const input2 = [1, 3, 7];

console.log(medianFromTwoArrays(input1, input2));

function medianFromTwoArrays(array1, array2) {
  const totalLength = array1.length + array2.length;

  return res;
}
